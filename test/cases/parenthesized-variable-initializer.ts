const index = (focusedIndex === -1 ? ports.length : focusedIndex) - 1
const arithmetic = (first + second) * third
const nested = ((first + second) * third)

const arrow = (value) => value
const typedArrow = (value: number): number => value
const multilineArrow = (
  value: number,
): number => value
const functionExpression = function () {}
const asyncFunctionExpression = async function () {}
const call = doThing()
