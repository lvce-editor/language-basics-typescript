import { value, type EntryView, type RowView as Row } from './timeline.ts'
import {
  type EntryView,
  // Preserve the import list across comments.
  type /* name */ RowView as Row,
  value,
} from './timeline.ts'
import type DefaultType from './default.ts'
import type { EntryView, RowView as Row } from './timeline.ts'
export type { EntryView, RowView as Row } from './timeline.ts'
export type * from './timeline.ts'
export /* list */ {
  value,
  // Preserve the export list across comments.
  type /* name */ EntryView as Entry,
  type RowView,
}
const following = 1
export type LocalType = string
export interface LocalInterface { value: string }
export const localValue = 1
