let texts: (string | null)[] = [];
let statuses: ("done" | "next" | "pending")[] = [];
let singleQuoted: ('done' | 'pending')[] = [];
let rows: (Row | null)[] = [];
let values: (number | boolean)[][] = [];
const ready = true;
// Highlighting continues after the union arrays.
